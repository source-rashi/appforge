{
  "name": "appforge",
  "version": "1.0.0",
  "private": true,
  "description": "AppForge — config-driven app generator monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=apps/api\" \"npm run dev --workspace=apps/web\"",
    "build": "npm run build --workspace=@appforge/config-types --workspace=@appforge/validators --workspace=@appforge/api",
    "test": "npm run test --workspace=apps/api",
    "lint": "npm run lint --workspaces --if-present",
    "type-check": "npm run type-check --workspaces --if-present",
    "start": "npm run start --workspace=apps/api",
    "clean": "rimraf **/node_modules **/.next **/dist **/build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
